<nav class="nav-quick">
  <ul class="reset nav-quick__list">
    {% for item in data %}
      <li class="nav-quick__item"><a class="link" href={{item.key}}>{{item.title}}</a></li>
    {% endfor %}
  </ul>
</nav>